# Vocabulary builder utility

## Features

- Adds words to list (vocab -a)
- Opens the vocabulary database for editing (vocab -e)
- Lists the words added (vocab -l)
- Displays help message (vocab / vocab -h)


### TODO

- Add a command-line argument parser
